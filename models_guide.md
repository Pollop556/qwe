# Топ 5 Моделей для Характеристик Студентов (Русский Язык)

Для задачи написания характеристик и запуска на слабом ноутбуке, я рекомендую модели размером 1.5B - 3B параметров. Они достаточно умные, чтобы писать связный текст, и достаточно легкие для Colab и обычных ноутбуков.

### 1. 🏆 Qwen/Qwen2.5-3B-Instruct (Мой выбор)
*   **Почему:** Лучшая модель в классе 3B на данный момент. Отлично понимает русский язык, следует инструкциям и пишет очень грамотно.
*   **Для ноутбука:** В формате GGUF (q4_k_m) занимает всего ~2 ГБ ОЗУ и работает очень быстро.

### 2. meta-llama/Llama-3.2-3B-Instruct
*   **Почему:** Официальная модель от Meta. Очень надежная, хорошее сообщество.
*   **Минус:** Чуть хуже работает с русским языком "из коробки" по сравнению с Qwen, но после дообучения (файн-тюнинга) этот минус уходит.

### 3. google/gemma-2-2b-it
*   **Почему:** Модель от Google. Очень "умная" для своего веса (всего 2 миллиарда параметров).
*   **Плюс:** Очень быстрая.

### 4. Qwen/Qwen2.5-1.5B-Instruct
*   **Почему:** Если ноутбук *совсем* слабый (например, 4 ГБ ОЗУ всего).
*   **Плюс:** Летает даже на калькуляторе.
*   **Минус:** Может иногда терять контекст или писать проще, чем 3B версия.

### 5. Vikhrmodels/Vikhr-Llama-3.2-1B-instruct (Твой текущий выбор)
*   **Почему:** Хорошая дообученная версия Llama 3.2.
*   **Минус:** 1 миллиард параметров — это маловато для сложных и красивых характеристик. Текст может быть суховатым.

---

# Инструкция: Как запустить обучение на Google Colab

Я обновил код `app.py` специально для Google Colab (среда T4 GPU). Теперь используется библиотека `unsloth`, которая ускоряет обучение в 2 раза и потребляет на 60% меньше памяти.

**Что делать:**
1. Загрузи файл `app.py` и `shuffled_dataset.csv` в Colab (или склонируй репозиторий).
2. Установи библиотеки перед запуском (первой ячейкой):
   ```bash
   pip install unsloth "xformers<0.0.27" "trl<0.9.0" peft accelerate bitsandbytes
   ```
3. Запусти код из `app.py`.
4. В конце обучения модель сохранится в папку `generic_student_characteristics_lora` и также в формате GGUF (файл `model-unsloth.Q4_K_M.gguf`).
5. **Скачай файл .gguf** себе на ноутбук. Это самая лучшая версия для слабого ПК!

# Инструкция: Как запустить на слабом ноутбуке

Для слабого ноутбука лучше всего использовать программу **LM Studio** или **KoboldCPP**. Это проще, чем писать код на Python, и работает быстрее.

1. Скачай [LM Studio](https://lmstudio.ai/).
2. Загрузи туда свой файл `.gguf`, который ты скачал из Colab.
3. Пиши в чат: "Напиши характеристику на студента Иванова..." и наслаждайся скоростью!

Если хочешь через Python (файл `bot.py`), я его тоже обновил. Он будет использовать библиотеку `llama-cpp-python` для запуска GGUF файлов, так как это стандарт для слабых ПК.
Тебе нужно будет установить: `pip install llama-cpp-python telebot`.
